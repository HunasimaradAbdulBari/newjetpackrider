import "./globals.css";

export const metadata = {
  title: "Quest Flight - Space Adventure Game",
  description: "Navigate through space, dodge obstacles, and test your knowledge in this exciting quiz game!",
  keywords: "quiz game, space game, educational game, phaser game, react game",
  viewport: "width=device-width, initial-scale=1",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <head>
        <meta name="theme-color" content="#1a1a2e" />
        <meta name="apple-mobile-web-app-capable" content="yes" />
        <meta name="apple-mobile-web-app-status-bar-style" content="default" />
        <meta name="mobile-web-app-capable" content="yes" />
      </head>
      <body className="antialiased">
        {children}
      </body>
    </html>
  );
}